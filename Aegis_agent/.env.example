# =============================================================================
# AEGIS AI - PRODUCTION CONFIGURATION FILE
# =============================================================================
# Copy this file to .env and configure your settings
# NO HARDCODED VALUES - Everything is configurable via this file
# =============================================================================

# =============================================================================
# API KEYS (Required)
# =============================================================================
# Master API key - used as fallback for all roles if specific keys not set
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Role-specific API keys for cost control and rate limit distribution
# If not set, falls back to OPENROUTER_API_KEY
STRATEGIC_API_KEY=
REASONING_API_KEY=
CODE_API_KEY=
VISUAL_API_KEY=

# =============================================================================
# MODEL CONFIGURATION
# =============================================================================
# All models are configurable - change these to use different LLMs
# Aegis v8.0 Full-Spectrum Architecture defaults:

# Strategic Model - for mission planning, triage, decision-making
# Default: deepseek/deepseek-r1 (DeepSeek R1 for deep reasoning)
STRATEGIC_MODEL=deepseek/deepseek-r1

# Reasoning Model - for vulnerability analysis, exploit planning
# Default: deepseek/deepseek-r1 (DeepSeek R1 for deep reasoning)
REASONING_MODEL=deepseek/deepseek-r1

# Code Model - for code analysis, payload generation
# Default: qwen/qwen-2.5-72b-instruct
CODE_MODEL=qwen/qwen-2.5-72b-instruct

# Visual Model - for screenshot analysis, UI reconnaissance
# Default: qwen/qwen2.5-vl-72b-instruct
VISUAL_MODEL=qwen/qwen2.5-vl-72b-instruct

# =============================================================================
# GENERATION PARAMETERS
# =============================================================================
# Default temperature for all LLM calls (0.0 to 1.0)
DEFAULT_TEMPERATURE=0.7

# Default maximum tokens for LLM responses
DEFAULT_MAX_TOKENS=4096

# =============================================================================
# SYSTEM PROMPTS (Optional - Advanced Configuration)
# =============================================================================
# Override default system prompts for each component
# If not set, uses built-in defaults from enhanced_ai_core.py
# 
# For multi-line prompts, use \n for newlines or set via Python dotenv parsing

# Triage system prompt - used when gathering mission information
# TRIAGE_SYSTEM_PROMPT=Your custom triage prompt here

# Code analysis system prompt - used when analyzing code for vulnerabilities
# CODE_ANALYSIS_SYSTEM_PROMPT=Your custom code analysis prompt here

# Payload generation system prompt - used when generating exploits
# PAYLOAD_GEN_SYSTEM_PROMPT=Your custom payload generation prompt here

# Verification system prompt - used for Devil's Advocate finding verification
# VERIFICATION_SYSTEM_PROMPT=Your custom verification prompt here

# Triage finding system prompt - used for AI-enhanced vulnerability triage
# TRIAGE_FINDING_SYSTEM_PROMPT=Your custom triage finding prompt here

# Fact extraction system prompt - used for extracting facts from tool outputs
# FACT_EXTRACTION_SYSTEM_PROMPT=Your custom fact extraction prompt here

# =============================================================================
# MEMORY & CONTEXT MANAGEMENT
# =============================================================================
# Maximum history entries before sliding window compression kicks in
# MAX_HISTORY_SIZE=10

# =============================================================================
# LOOP DETECTION & SAFETY
# =============================================================================
# Window size for action loop detection
# LOOP_DETECTION_WINDOW=3

# Threshold for loop detection (occurrences in window)
# LOOP_OCCURRENCE_THRESHOLD=2

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Path to SQLite database for mission tracking
# DATABASE_PATH=data/aegis_mission.db

# =============================================================================
# VISUAL RECON / STEALTH MODE
# =============================================================================
# Custom user agent for stealth mode (to bypass WAF/bot detection)
# Update this periodically to match recent Chrome versions
# STEALTH_USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/121.0.0.0 Safari/537.36

# =============================================================================
# LOGGING
# =============================================================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# =============================================================================
# OPTIONAL: LOCAL MODEL SUPPORT
# =============================================================================
# Uncomment to use local models via Ollama instead of OpenRouter
# LOCAL_MODEL_MODE=true
# OLLAMA_HOST=http://localhost:11434

# =============================================================================
# NOTES
# =============================================================================
# 1. API keys are sensitive - never commit .env to version control
# 2. All models can be swapped without code changes via this file
# 3. System prompts can be customized for specific use cases
# 4. Default values are optimized for general penetration testing
# 5. For production, consider using separate API keys per role for cost tracking
