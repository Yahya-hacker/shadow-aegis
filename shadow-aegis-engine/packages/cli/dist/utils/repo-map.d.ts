/**
 * Generates a compressed repository map for the given directory.
 * Returns a single string containing all structural signatures.
 */
export declare function generateRepoMap(targetPath: string): Promise<string>;
